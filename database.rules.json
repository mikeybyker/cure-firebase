{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "ranks": {
        ".read": "true",
        ".write": "auth != null",
        ".indexOn": ["id", "average"]
    },
    "users": {
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    }
  }
}